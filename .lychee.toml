# Lychee link checker configuration
# https://lychee.cli.rs/

# Exclude placeholder and development URLs
exclude = [
    # Localhost (development)
    "^http://localhost",
    "^https://localhost",
    # Example domains (placeholder URLs in documentation)
    "example\\.com",
    # Placeholder CDN URLs
    "your-cdn\\.com",
    # VitePress root-relative links (/guide/foo, /admin/bar, etc.)
    # These are validated by the VitePress build (Docs Build CI job).
    "^/[a-zA-Z]",
]

# Don't check links in code blocks
include_verbatim = false
