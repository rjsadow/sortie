{{- if and .Values.auth.enabled (not .Values.auth.existingSecret) }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "launchpad.fullname" . }}-auth
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "launchpad.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- if .Values.auth.jwtSecret }}
  LAUNCHPAD_JWT_SECRET: {{ .Values.auth.jwtSecret | quote }}
  {{- else }}
  # WARNING: Auto-generated secret - not recommended for production
  LAUNCHPAD_JWT_SECRET: {{ randAlphaNum 32 | quote }}
  {{- end }}
  LAUNCHPAD_JWT_ACCESS_EXPIRY: {{ .Values.auth.accessExpiry | quote }}
  LAUNCHPAD_JWT_REFRESH_EXPIRY: {{ .Values.auth.refreshExpiry | quote }}
  LAUNCHPAD_ADMIN_USERNAME: {{ .Values.auth.adminUsername | quote }}
  {{- if .Values.auth.adminPassword }}
  LAUNCHPAD_ADMIN_PASSWORD: {{ .Values.auth.adminPassword | quote }}
  {{- end }}
  LAUNCHPAD_ALLOW_REGISTRATION: {{ .Values.auth.allowRegistration | quote }}
{{- end }}
